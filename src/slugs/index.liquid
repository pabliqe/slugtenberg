<!doctype html>
<html lang="{{ config.siteLanguage }}">
  <head>
    {% render 'head' %}
  </head>
  <body class="slug--{{ current }}">
  
    <h1>{{ title }}</h1>
    
    {% render 'foot' %}
  </body>
</html>